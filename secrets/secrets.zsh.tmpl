export ANTHROPIC_API_KEY="{{ op://Personal/Dev Secrets/anthropic-api-key }}"
export OPENAI_API_KEY="{{ op://Personal/Dev Secrets/openai-api-key }}"
export HF_TOKEN="{{ op://Personal/Dev Secrets/hf-token }}"
export WANDB_API_KEY="{{ op://Personal/Dev Secrets/wandb-api-key }}"
export AXIOM_API_KEY="{{ op://Personal/Dev Secrets/axiom-api-key }}"
export TAILSCALE_OAUTH_CLIENT_SECRET="{{ op://Personal/Dev Secrets/tailscale-oauth-client-secret }}"
export MULLVAD_ACCOUNT_NUMBER="{{ op://Personal/Dev Secrets/mullvad-account-number }}"
export DOCKER_PAT="{{ op://Personal/Dev Secrets/docker-pat }}"
export EXA_API_KEY="{{ op://Personal/Dev Secrets/exa-api-key }}"
export OPENROUTER_API_KEY="{{ op://Personal/Dev Secrets/openrouter-api-key }}"
# Alpaca — dipbot brokerage. v1 uses paper (sandbox) keys only; live keys
# stay commented out until Phase 8 validates.
export ALPACA_PAPER_KEY_ID="{{ op://Personal/Dev Secrets/alpaca-paper-key-sandbox }}"
export ALPACA_PAPER_SECRET_KEY="{{ op://Personal/Dev Secrets/alpaca-paper-secret-sandbox }}"
# export ALPACA_LIVE_KEY_ID="{{ op://Personal/Dev Secrets/alpaca-paper-key-live }}"
# export ALPACA_LIVE_SECRET_KEY="{{ op://Personal/Dev Secrets/alpaca-paper-secret-live }}"
# Shared secret for the dipbot GitHub release webhook. Set the same value
# in the GitHub repo / org webhook settings; HMAC-SHA256 verifies on the
# daemon side. Generate with `openssl rand -hex 32`.
export GITHUB_WEBHOOK_SECRET="{{ op://Personal/Dev Secrets/dipbot-github-webhook-secret }}"
