# Canonical copy of the org-wide Devin blueprint -- the reviewed source of truth.
# Org/enterprise blueprints can't be git-backed (they run before any repo is
# cloned; this one is what clones dots), so this file is reviewed here and
# mirrored to Devin's org settings (Settings > Environment) via the approval
# flow rather than auto-synced.
#
# It is deliberately a thin bootstrap: install Nix, clone dots, activate the
# `devin-cloud` home-manager host. Everything substantive -- toolchain, shell,
# ~/.agents + skills, tailscale + op (via nix), and the devin-tailscale-up /
# devin-op-ssh helpers -- lives in dots (hosts/devin-cloud), not here.
#
# No `set -e`: this runs before EVERY repo build in the org, so each block
# degrades gracefully and never fails an unrelated repo's build.
initialize:
  # devin-oidc mints the session's short-lived OIDC token. devin-tailscale-up
  # hands it to Tailscale (workload identity federation) instead of holding a
  # Tailscale auth key / OAuth secret on the VM.
  - uses: github.com/CognitionAI/actions/setup-devin-oidc@main
  - name: nix + dots devin-cloud host
    run: |
      # 1. Determinate Nix (the /nix store + profile persist into the snapshot).
      if [ ! -e /nix/var/nix/profiles/default/bin/nix ]; then
        curl --proto '=https' --tlsv1.2 -sSf -L https://install.determinate.systems/nix \
          | sh -s -- install linux --no-confirm --init none || true
      fi
      if [ -e /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh ]; then
        . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
      fi
      # nix-daemon has no init service (--init none) and a snapshot boots with no
      # processes, so start it here for the build and again from $ENVRC (below)
      # for sessions that run nix itself (`just check`, `nix build`).
      if command -v nix >/dev/null 2>&1 && ! nix store info >/dev/null 2>&1; then
        sudo sh -c 'nohup /nix/var/nix/profiles/default/bin/nix-daemon >/tmp/nix-daemon.log 2>&1 &'
        sleep 3
      fi

      # 2. Clone dots + activate the devin-cloud home-manager host. This installs the
      #    toolchain, shell config, ~/.agents + skills, tailscale + op (via nix), and
      #    the devin-tailscale-up / devin-op-ssh helpers onto PATH.
      DOTS="$HOME/all/dots"
      if [ ! -d "$DOTS/.git" ]; then
        mkdir -p "$HOME/all"
        git clone --depth 1 https://github.com/charliemeyer2000/dots "$DOTS" || true
      else
        git -C "$DOTS" pull --ff-only || true
      fi
      if command -v nix >/dev/null 2>&1 && \
         nix build "$DOTS#homeConfigurations.devin-cloud.activationPackage" \
           --no-link --print-out-paths >/tmp/hm-out 2>/tmp/hm-build.log; then
        HOME_MANAGER_BACKUP_EXT=bak "$(cat /tmp/hm-out)/activate" || true
        # Put the home-manager profile (tools + session vars) on PATH. Devin runs
        # commands in non-interactive, non-login shells that source neither .bashrc
        # nor .profile -- it sources $ENVRC. Writing there is what actually exposes
        # the toolchain + helpers to the agent; the rc files are for interactive use.
        LINE='[ -e "$HOME/.nix-profile/etc/profile.d/hm-session-vars.sh" ] && . "$HOME/.nix-profile/etc/profile.d/hm-session-vars.sh"; export PATH="$HOME/.nix-profile/bin:$PATH"'
        for rc in ${ENVRC:+"$ENVRC"} "$HOME/.bashrc" "$HOME/.profile"; do
          touch "$rc"
          grep -qF 'nix-profile/bin:$PATH' "$rc" || printf '\n%s\n' "$LINE" >> "$rc"
        done
        # Every session is a tailnet node by default: the first command Devin runs
        # joins (a few seconds), later ones are a ~20 ms no-op. Only $ENVRC -- the
        # agent's shells -- not the interactive rc files.
        if [ -n "${ENVRC:-}" ]; then
          JOIN='command -v devin-tailscale-up >/dev/null 2>&1 && devin-tailscale-up --auto || true'
          grep -qF 'devin-tailscale-up --auto' "$ENVRC" || printf '%s\n' "$JOIN" >> "$ENVRC"
          # `nix` on PATH + the daemon up (pgrep is ~5 ms; the start runs once per boot).
          NIXD="[ -e /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh ] && . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh; pgrep -x nix-daemon >/dev/null 2>&1 || sudo sh -c 'nohup /nix/var/nix/profiles/default/bin/nix-daemon >/tmp/nix-daemon.log 2>&1 &'"
          grep -qF 'pgrep -x nix-daemon' "$ENVRC" || printf '%s\n' "$NIXD" >> "$ENVRC"
        fi
      else
        echo "WARN: devin-cloud host not built -- see /tmp/hm-build.log" >&2
      fi
knowledge:
  - name: local-cloud-parity
    contents: |
      This VM mirrors Charlie's local dots setup via the devin-cloud
      home-manager host (activated at snapshot build): same shell, aliases, CLI
      tools, ~/.agents + skills. Tools live in ~/.nix-profile/bin. Source of
      truth: charliemeyer2000/dots -> hosts/devin-cloud. Most secrets are
      Devin-managed org env vars; OP_SERVICE_ACCOUNT_TOKEN also grants on-demand
      access to Charlie's 1Password "Developer" vault.
  - name: tailnet-access
    contents: |
      This VM joins Charlie's tailnet on its first shell command: $ENVRC runs
      `devin-tailscale-up --auto` before every command, which exchanges the
      session's Devin OIDC token with Tailscale (workload identity federation;
      TS_DEVIN_OIDC_CLIENT_ID org var, not a secret) and joins as an ephemeral
      tag:devin node -- auto-removed at session end, ACL-scoped to life:443 +
      workstation:22/8080. After that the life gateway
      (https://life.tail0eb43d.ts.net/mcp; the `life` MCP server) and the
      workstation (aiworkstation: hostd :8080, Grafana, Tailscale SSH as
      charlie) just work. The `life` MCP client connects at session start,
      before any shell command, so its first call can fail with "error sending
      request" -- that is the tailnet, not LIFE_MCP_API_KEY: run
      devin-tailscale-up by hand (it prints why a join fails), then call the
      MCP again; never curl or script against the gateway. There is no
      Tailscale secret on this VM. Deploys to AWS/Vercel go through CI -- open
      a PR, don't hold creds.
  - name: machine-ssh-1password
    contents: |
      Hosts that are NOT on the tailnet need Charlie's SSH key, loaded from
      1Password on demand:
        . devin-op-ssh      # source it so SSH_AUTH_SOCK is exported
        ssh <user>@<host>
      Needs the OP_SERVICE_ACCOUNT_TOKEN org secret; key read from
      op://Developer/id_ed25519 into ssh-agent, never written to disk. That vault
      also holds API keys (Anthropic, OpenAI, OpenRouter, Exa, GitHub, Docker,
      HF, WandB, Axiom, Alpaca, Tailscale, E2B):
        op read "op://Developer/<item>/credential"
